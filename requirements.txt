# Pinned versions tested with the headline experiments in the paper
# (Apr 2026 reproduction). Python 3.13 + CUDA 12.x.
#
# Install:  pip install -r requirements.txt
# Note: torch is install-target-specific (CUDA wheel); see
# https://pytorch.org/get-started/locally/ for the right index URL.
# Once the upstream wheel is selected, pip will resolve the rest.

# Deep learning core
torch==2.8.0
torchvision
accelerate==1.10.1

# HuggingFace stack — transformers 5.x is required (Qwen3.5/Gemma 4 support)
transformers==5.6.2
huggingface_hub==1.12.0
datasets==4.5.0
sentencepiece==0.2.1

# Vision / image processing
Pillow==11.3.0
opencv-python-headless==4.13.0.92
scikit-image
easyocr==1.7.2
pytesseract  # optional second OCR backend (system tesseract binary needed)

# Numerics / dataframe
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
scikit-learn

# Plotting
matplotlib

# Utilities
tqdm==4.67.3
pyyaml
einops
rich
python-dotenv
